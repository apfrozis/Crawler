

let results = require('./crawl_results');
let crawler = require('./crawler30');

/*
    Correr estas duas duas as 6 da manha
*/

// crawler.games()

setTimeout(() => {
   
    crawler.games()
   
}, 1000*2);
